set -g status off

bind -T root F3 {
  set -g prefix None
  set -g key-table off
  set -g status off
  refresh-client -S
}

bind -T off F3 {
  set -g -u prefix
  set -g -u key-table
  set -g -u status
  refresh-client -S
}

